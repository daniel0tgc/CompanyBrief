# Done.md — CompanyBrief
<!--
  WRITTEN BY: Cursor Agent
  READ BY: Claude Code (to generate next phase batch) and the next Cursor Agent (to pick up state)

  RULES:
  - Check off task boxes IMMEDIATELY after completing each task — not at end of phase
  - Append-only — never delete entries
  - Phase summary MUST be written before stopping after a phase
  - Codebase State Graph must reflect current reality — this is ground truth
  - Be specific: exact file paths, exact errors, exact deviations
-->

---

## Project State

**App:** CompanyBrief
**Started:** 2026-04-14
**Last updated:** 2026-04-15
**Web URL:** https://web-production-f5f8.up.railway.app
**API URL:** https://api-production-7bed.up.railway.app
**Current phase:** Phase 7 complete — Phase 8 next
**Start method:** Scaffold from scratch

---

## Architecture Decisions

- Platform: Web (Next.js 14 App Router) — not React Native
- Auth: NextAuth v5 (Auth.js) with Google provider → httpOnly JWT cookie; `NEXTAUTH_SECRET` shared with Fastify for server-to-server token verification
- No Stripe — free product
- AI: Anthropic `claude-sonnet-4-6` with tool use, runs in BullMQ workers only
- SSE bridge: BullMQ job → Redis pub/sub → Fastify SSE route → browser EventSource
- SSE auth: NextAuth token passed as `?token=` query param (EventSource cannot set headers)
- DB: PostgreSQL + JSONB for `companies.analysis` — no MongoDB
- Agent system prompt: loaded from `ANALYSIS_CONTEXT.md` file at startup, cached in module scope

---

## Phases Complete

- [x] Phase 1: Monorepo scaffold + Railway deploy
- [x] Phase 2: Google OAuth (NextAuth v5)
- [x] Phase 3: Company CRUD + dashboard shell
- [x] Phase 4: Analysis agent (Anthropic tool use)
- [x] Phase 5: SSE streaming (Redis pub/sub → EventSource)
- [x] Phase 6: Analysis UI (section cards + agent thinking)
- [x] Phase 7: Chatbot + expansion cards
- [ ] Phase 8: Dashboard polish + company management
- [ ] Phase 9: Hardening

---

## Required Keys

| Variable | Service | Used In | Railway |
|---|---|---|---|
| `DATABASE_URL` | api | Postgres | ⬜ |
| `REDIS_URL` | api | Redis + BullMQ + pub/sub | ⬜ |
| `NEXTAUTH_SECRET` | api | NextAuth JWT verification | ⬜ |
| `ANTHROPIC_API_KEY` | api | Anthropic SDK | ⬜ |
| `TAVILY_API_KEY` | api | Web search + news + extract | ⬜ |
| `NEXT_PUBLIC_APP_URL` | api | CORS + OAuth redirect | ⬜ |
| `NEXTAUTH_URL` | web | NextAuth | ⬜ |
| `NEXTAUTH_SECRET` | web | NextAuth (must match api value) | ⬜ |
| `GOOGLE_CLIENT_ID` | web | Google OAuth | ⬜ |
| `GOOGLE_CLIENT_SECRET` | web | Google OAuth | ⬜ |
| `NEXT_PUBLIC_API_URL` | web | Fastify base URL | ⬜ |
| `SENTRY_DSN` | both | Error monitoring (Phase 9) | ⬜ |

---

## Phase Log

---

### Phase 1 — Monorepo Scaffold + Railway Deploy
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 1.1 — Initialize monorepo
- [x] 1.2 — Scaffold Next.js app
- [x] 1.3 — Scaffold Fastify API
- [x] 1.4 — Database schema + migration
- [x] 1.5 — Fixture data
- [x] 1.6 — Tailwind config + Inter font
- [x] 1.7 — Environment variable files
- [x] 1.8 — Railway deploy

#### Deviations
- 1.1: Used existing `CompanyBrief` repo root (not new `companybrief/` folder). Enabled pnpm via `corepack`.
- 1.2: `create-next-app` install failed until pnpm was activated; `shadcn@latest init --defaults` (v4) added Geist + shadcn CSS tokens — Inter + Context theme applied in task 1.6.
- 1.3: Registered `@fastify/cors` only in `server.ts` (JWT/rate-limit installed for later phases). Listen uses `0.0.0.0` for Railway.
- 1.4: `conversations.messages` default uses Drizzle `sql` for empty JSON array. Initial migration filename auto-generated by drizzle-kit (`0000_unknown_mongoose.sql`).
- 1.5: No bundled `company_output_example` file in repo; seed uses `vercelDemoAnalysis` — full synthetic JSON matching `CompanyAnalysis`.
- 1.6: Extended existing Tailwind v3 config (shadcn baseline) with Context theme tokens; `primary` now matches UI blue (`#3B82F6`). Post-phase: stripped `outline-ring/50` from `globals.css` `*` rule so `next build` succeeds (Tailwind v3).
- 1.7: Dev `ANTHROPIC_API_KEY` / `TAVILY_API_KEY` / Google OAuth keys left empty until later phases.
- 1.8: `railway add --database postgres` returned "Unauthorized" (CLI v4 bug). Databases provisioned via Railway GraphQL API directly (`serviceCreate` with `ghcr.io/railwayapp-templates/postgres-ssl:16` + `redis:7-alpine`). Postgres volume required `PGDATA=/var/lib/postgresql/data/pgdata` subdirectory to avoid `lost+found` conflict at volume root. All four services `SUCCESS`.

#### Phase Summary
The repository is a pnpm monorepo with `apps/web` (Next.js 14 App Router, Tailwind, shadcn/ui defaults, Inter + Context color tokens) and `packages/api` (Fastify 5 on port 3001 with CORS and `GET /health`). Drizzle schema matches Context (users, companies, conversations, expansion_cards), an initial SQL migration was generated, and a seed script upserts a demo user plus a Vercel company row with a full synthetic `CompanyAnalysis` JSON. Example env files and local `.env` / `.env.local` are present; real secrets stay empty until later phases. Railway project `companybrief` created with four services (Postgres, Redis, api, web); all four deployed and returning HTTP 200 in production. `GET /health` → `{"status":"ok"}`. Next.js home page loads at web URL.

#### Files Created
```
.gitignore
package.json
pnpm-workspace.yaml
pnpm-lock.yaml
apps/web/.env.example
apps/web/.env.local
apps/web/.eslintrc.json
apps/web/.gitignore
apps/web/README.md
apps/web/app/favicon.ico
apps/web/app/fonts/GeistMonoVF.woff
apps/web/app/fonts/GeistVF.woff
apps/web/app/globals.css
apps/web/app/layout.tsx
apps/web/app/page.tsx
apps/web/components.json
apps/web/components/ui/button.tsx
apps/web/lib/utils.ts
apps/web/next-env.d.ts
apps/web/next.config.mjs
apps/web/package.json
apps/web/postcss.config.mjs
apps/web/tailwind.config.ts
apps/web/tsconfig.json
packages/api/.env.example
packages/api/.env
packages/api/drizzle.config.ts
packages/api/package.json
packages/api/tsconfig.json
packages/api/src/server.ts
packages/api/src/db/schema.ts
packages/api/src/db/client.ts
packages/api/src/db/fixtures/seed.ts
packages/api/src/db/migrations/0000_unknown_mongoose.sql
packages/api/src/db/migrations/meta/_journal.json
packages/api/src/db/migrations/meta/0000_snapshot.json
packages/api/src/lib/types.ts
```

#### Dependencies Installed
```
apps/web: @base-ui/react, class-variance-authority, clsx, lucide-react, next, react, react-dom, shadcn, tailwind-merge, tw-animate-css; dev: @types/node, @types/react, @types/react-dom, eslint, eslint-config-next, postcss, tailwindcss, typescript
packages/api: @fastify/cors, @fastify/jwt, @fastify/rate-limit, dotenv, drizzle-orm, fastify, pg, zod; dev: @types/node, @types/pg, drizzle-kit, tsx, typescript
```

#### Issues Encountered
`create-next-app` initially failed under sandbox (EPERM) and without pnpm on PATH; resolved with full permissions and `corepack prepare pnpm@9.15.0`. A nested `.git` inside `apps/web/` from scaffolding was removed so only the monorepo root is a Git repo. `next build` failed on shadcn’s `@apply border-border outline-ring/50` because Tailwind v3 did not resolve `outline-ring/50`; fixed by dropping `outline-ring/50` from the universal `*` rule in `apps/web/app/globals.css` . Railway CLI v4 `railway add --database postgres` returned Unauthorized; Postgres + Redis provisioned via Railway GraphQL API (`serviceCreate` with `ghcr.io/railwayapp-templates/postgres-ssl:16` + `redis:7-alpine`). Postgres volume crashed twice: first mount at `/var/lib/postgresql/data` blocked `initdb` due to `lost+found` at volume root; second mount at `/var/lib/postgresql` rejected by image validator. Fixed by `PGDATA=/var/lib/postgresql/data/pgdata` with mount back at `/var/lib/postgresql/data`. All four services reached SUCCESS.

#### Deploy Status
- Web URL: **https://web-production-f5f8.up.railway.app** → HTTP 200 ✅
- API URL: **https://api-production-7bed.up.railway.app** → HTTP 200 ✅
- `GET /health` → **`{"status":"ok","timestamp":"2026-04-15T04:06:54.755Z"}`** ✅

#### Phase 1 verification checklist (Context L354–359)
- [x] `tsc --noEmit` — zero errors in `apps/web` and `packages/api`
- [x] `pnpm dev` — Next.js listens on **3000** and Fastify on **3001** (parallel start confirmed; process ended with SIGTERM in smoke test)
- [x] `GET /health` → `{ "status": "ok", "timestamp": "…" }` locally (production `node dist/server.js`)
- [x] `.env.example` files present in repo; `packages/api/.env` and `apps/web/.env.local` gitignored
- [x] Done.md Phase 1 summary written; Railway acceptance confirmed — `GET /health` → 200 ✅, Next.js home page → 200 ✅

#### Notes for Claude Code
Phase 1 complete. Railway project `companybrief` (ID `bd68eb63-d7a9-4a28-84f3-c8535e6d4283`), production env `7a5c4741-f38f-4963-a1cc-7c91edee757f`. Services: Postgres (`811732d1`), Redis (`c21fe24b`), api (`be6a309c`), web (`cb983020`). `NEXTAUTH_SECRET` identical on both api and web in Railway. `ANTHROPIC_API_KEY`, `TAVILY_API_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` are empty strings in Railway — fill before Phase 4 and Phase 2 respectively. Run `db:migrate` and `db:seed` against Railway Postgres before Phase 3. `packages/api/src/db/client.ts` is not imported by `server.ts` yet — safe for now. Build commands use `corepack enable && pnpm install --no-frozen-lockfile` since corepack isn't installed by default in Nixpacks.
---

### Phase 2 — Authentication (NextAuth v5 + Google)
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 2.1 — NextAuth config
- [x] 2.2 — NextAuth route handler
- [x] 2.3 — Next.js middleware (auth gate)
- [x] 2.4 — Sign-in page
- [x] 2.5 — Fastify auth plugin (verify NextAuth JWT)
- [x] 2.6 — Users repository
- [x] 2.7 — GET /auth/me route
- [x] 2.8 — API client in Next.js

#### Deviations
- 2.1: Added `declare module "next-auth"` inline in auth.ts for session.user.googleId augmentation.
- 2.2: none
- 2.3: none
- 2.4: `"use client"` — uses `next-auth/react` signIn; Google icon inline SVG.
- 2.5: Used `jose` (`jwtDecrypt`) not `jsonwebtoken` — NextAuth v5 produces JWE tokens. Key = SHA-256(NEXTAUTH_SECRET). Also accepts `?token=` query param for Phase 5 SSE.
- 2.6: `onConflictDoUpdate` on email for upsert.
- 2.7: Dynamic import of usersRepository inside handler.
- 2.8: Raw token from `next-auth.session-token` cookie via `next/headers`.

#### Phase Summary
Google OAuth sign-in works end-to-end. NextAuth v5 beta installed; `auth.ts` exports handlers, auth, signIn, signOut with JWT session strategy. Route handler at `/api/auth/[...nextauth]`. Middleware protects all routes; unauthenticated requests → `/sign-in`. Sign-in page calls `signIn('google', { callbackUrl: '/dashboard' })`. Fastify auth plugin decrypts NextAuth JWE with `jose`, finds-or-creates user in Postgres, attaches `req.user`. `GET /auth/me` → 401 with no token confirmed live. `lib/api.ts` reads raw session cookie and forwards as `Authorization: Bearer`.

#### Files Created
```
apps/web/lib/auth.ts
apps/web/app/api/auth/[...nextauth]/route.ts
apps/web/middleware.ts
apps/web/app/(auth)/sign-in/page.tsx
apps/web/lib/api.ts
packages/api/src/plugins/auth.ts
packages/api/src/db/repository/users.ts
packages/api/src/routes/auth.ts
```

#### Issues Encountered
NextAuth v5 uses JWE (not JWS) by default — `jsonwebtoken` cannot decode it. Switched to `jose`'s `jwtDecrypt`. `fastify-plugin` installed separately. `auth()` returns Session not raw JWT; raw token read from cookie store in `lib/api.ts`.

#### Notes for Claude Code
Register the Google OAuth callback URL in Google Cloud Console: `https://web-production-f5f8.up.railway.app/api/auth/callback/google` (production) and `http://localhost:3000/api/auth/callback/google` (local). DB migration has NOT been run against Railway Postgres yet — run `pnpm db:migrate` before Phase 3 data work. `req.user` is typed on `FastifyRequest` in `plugins/auth.ts`. `usersRepository.findOrCreate` upserts on email conflict.

---

### Phase 3 — Company CRUD + Dashboard Shell
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 3.1 — Companies repository
- [x] 3.2 — Companies routes (CRUD)
- [x] 3.3 — BullMQ queue setup (stub)
- [x] 3.4 — Sidebar layout + company list
- [x] 3.5 — Dashboard page
- [x] 3.6 — Landing page search input

#### Deviations
- 3.1: `findAllByUser` uses explicit column select (omits `analysis` JSONB). `CompanyListItem` type exported.
- 3.2: Slugify strips non-alphanumeric + collapses hyphens. `GET /companies/:id` ownership check returns 404 (not 403) for security.
- 3.3: `redis.ts` uses named import `Redis as IORedis` (not default import). Worker started via `startAnalysisWorker()` called from `server.ts`. `ioredis` option `maxRetriesPerRequest: null` required by BullMQ.
- 3.4: Sidebar uses `"use client"` `CompanyListItem` for `usePathname` active-state detection. `(app)/layout.tsx` silently catches `apiClient` errors (unauthenticated → middleware handles redirect anyway).
- 3.5: Dashboard and company page added. Company `[id]` page is a stub showing status/tagline — full analysis UI in Phase 6.
- 3.6: Removed erroneous file-level `"use server"` directive from `page.tsx` — inline `"use server"` inside `submitCompany` is correct. Also added `/api/healthz` route for Railway healthcheck (middleware excludes `/api/*`).

#### Phase Summary
Companies CRUD is live. `GET/POST/DELETE /companies` all protected (401 without auth). BullMQ `analysis` queue wired to ioredis; stub worker simulates 2s processing, sets status `running` then `complete` with placeholder analysis. Sidebar layout (`(app)/layout.tsx`) fetches companies server-side and renders `<Sidebar>` with status badges. Dashboard grid renders company cards. Landing page has company name form with server action → `POST /companies` → redirect to `/company/[id]`. All routes protected by `middleware.ts`. Railway web service healthcheck moved to `/api/healthz` (always 200, excluded from auth middleware).

#### Files Created
```
apps/web/lib/types.ts
apps/web/app/(app)/layout.tsx
apps/web/app/(app)/dashboard/page.tsx
apps/web/app/(app)/company/[id]/page.tsx
apps/web/app/api/healthz/route.ts
apps/web/components/sidebar/Sidebar.tsx
apps/web/components/sidebar/CompanyListItem.tsx
packages/api/src/lib/redis.ts
packages/api/src/lib/queue.ts
packages/api/src/db/repository/companies.ts
packages/api/src/routes/companies.ts
packages/api/src/jobs/analysis.job.ts
```

#### Issues Encountered
File-level `"use server"` on `page.tsx` caused `next build` to fail (Next.js treats ALL exports as server actions, but `LandingPage` is synchronous). Railway silently served Phase 2 code across 5 failed deploys because the build error was not surfaced in the CLI logs — only visible via `buildLogs` GraphQL query. Fixed by removing the top-level directive. Railway build cache is keyed on `package.json` — bumped version to 0.3.0 to force rebuild. `AUTH_TRUST_HOST=1` and `AUTH_SECRET` added to Railway web service env vars (belt-and-suspenders alongside `trustHost: true` in auth.ts). DB migration configured as `preDeployCommand` on the API Railway service.

#### Notes for Claude Code
BullMQ queue name is `'analysis'`. Redis connection uses `maxRetriesPerRequest: null`. Worker is started in `server.ts` via `startAnalysisWorker()`. The preDeployCommand `pnpm --filter @companybrief/api run db:migrate` runs on every API deploy (idempotent via Drizzle journal). Phase 4 replaces the stub in `analysis.job.ts` with the real agent. The `(app)/company/[id]/page.tsx` is a stub — Phase 6 replaces it with full analysis UI.

---

### Phase 4 — Analysis Agent (Anthropic Tool Use)
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 4.1 — Tavily client
- [x] 4.2 — Scraper utility
- [x] 4.3 — Anthropic tool definitions
- [x] 4.4 — ANALYSIS_CONTEXT.md (agent system prompt)
- [x] 4.5 — Agent orchestrator
- [x] 4.6 — Replace stub job with real agent

#### Deviations
- 4.1: none
- 4.2: Used native `fetch` (Node 18+ built-in) instead of `node-fetch` — avoids extra dependency, functionally identical. `AbortSignal.timeout(10_000)` used for cheerio scrape timeout.
- 4.3: none
- 4.4: none
- 4.5: Section failures are caught individually (per-section try/catch) so one tool failure doesn't abort the entire analysis — partial analysis is saved if most sections succeed. JSON extracted from response using indexOf/lastIndexOf to handle any preamble the model might add despite the output rule.
- 4.6: Redis publish is inlined in `analysis.job.ts` using the existing `redis` singleton (not via `redis-pubsub.ts`) because Phase 5 creates that helper. The inline emit uses `.catch()` so a publish failure never crashes the job.

#### Phase Summary
The real analysis agent is live and wired into the BullMQ worker. `runAnalysisAgent` in `packages/api/src/lib/agent/index.ts` loads `ANALYSIS_CONTEXT.md` at startup (cached in module scope), iterates over all 13 sections in order, and for each section runs a multi-turn Anthropic tool-use loop until `stop_reason === 'end_turn'`. The three tools (`web_search`, `scrape_url`, `search_news`) call Tavily and cheerio respectively. Each tool call and result, each thinking text block, and each completed section emits an `AgentEvent` to Redis pub/sub channel `analysis:[companyId]`. `analysis.job.ts` stub was fully replaced — it sets status `running`, calls the agent, saves the result, and calls `updateError` on failure.

#### Files Created
```
packages/api/src/lib/tavily.ts
packages/api/src/lib/scraper.ts
packages/api/src/lib/agent/tools.ts
packages/api/src/lib/agent/ANALYSIS_CONTEXT.md
packages/api/src/lib/agent/index.ts
```

#### Files Modified
```
packages/api/src/jobs/analysis.job.ts   (stub replaced with real agent call)
```

#### Dependencies Added
```
packages/api: @anthropic-ai/sdk, cheerio
```

#### Issues Encountered
None at build time. `tsc --noEmit` passed with zero errors in both packages. No linter errors. Live API verification pending real `ANTHROPIC_API_KEY` and `TAVILY_API_KEY` on Railway.

#### Post-Phase Fix (2026-04-15)
- **Bug:** NextAuth v5 beta.31 renamed session cookies from `next-auth.*` to `authjs.*`. `getRawToken()` in `apps/web/lib/api.ts` was checking only the old v4 names → returned null on every call → no Authorization header sent to Fastify → 401 on all authenticated routes → Application error on every page load.
- **Fix:** Added `authjs.session-token` and `__Secure-authjs.session-token` as the primary lookups (v4 names kept as fallback). Deployed to Railway web service → SUCCESS.
- **File modified:** `apps/web/lib/api.ts` — `getRawToken()` now checks all four cookie name variants.

#### Notes for Claude Code
- `ANTHROPIC_API_KEY` and `TAVILY_API_KEY` must be set in Railway api service env before Phase 4 agent runs in production — they were empty strings from Phase 1.
- Redis pub/sub publish is inline in `analysis.job.ts` using the existing `redis` singleton; Phase 5 task 5.1 extracts this into `redis-pubsub.ts` and adds the subscribe side.
- The `emit` function signature (`AgentEvent`) is exported from `agent/index.ts` — both the job and the future SSE route import it from there.
- Agent runs section-by-section; each section is one `messages.create` call with its own conversation. This keeps context windows small but means the model doesn't cross-reference sections automatically.
- `tavilyExtract` is the fallback when cheerio returns < 200 chars or the fetch fails. If Tavily rate-limits, the tool catches and returns the error string as the result content — the agent will note it in output rather than crashing.
- Average analysis time will depend on Anthropic latency × 13 sections × tool calls per section. Expect 60–120s per company.

---

### Phase 5 — SSE Streaming
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 5.1 — Redis pub/sub helpers
- [x] 5.2 — SSE route
- [x] 5.3 — Frontend EventSource hook
- [x] 5.4 — SSE token auth (query param)
- [x] 5.5 — Wire up end-to-end

#### Deviations
- 5.1: none — publisher uses singleton ioredis client; each subscriber call creates a new dedicated client (ioredis subscriber mode constraint)
- 5.2: Used `reply.hijack()` to prevent Fastify from sending its own response after the handler. Added `X-Accel-Buffering: no` header for Railway's nginx reverse proxy. Returns a promise that awaits the `close` event on `request.raw` to keep the connection alive until client disconnects.
- 5.3: `useAnalysisStream` takes `token` as a second parameter (deviation from Context.md spec which only has `companyId`). EventSource cannot set headers, so the token must come from the server component via props. The hook adds `"use client"` directive at the top of the file (not file-level `"use server"` — correct).
- 5.4: Already implemented in Phase 2 `verifyNextAuthToken` — `?token=` query param accepted as fallback to Authorization header. No changes needed.
- 5.5: Pre-work: fixed API build script to copy `ANALYSIS_CONTEXT.md` to `dist/lib/agent/` (`tsc` does not copy non-TS files). This was a Phase 4 oversight that would have crashed the API on Railway startup.

#### Phase Summary
SSE streaming is fully wired from BullMQ worker → Redis pub/sub → Fastify SSE route → browser EventSource. `redis-pubsub.ts` extracts the publish/subscribe logic from the job file and provides typed `publishAgentEvent` and `subscribeToAnalysis` helpers. The SSE route at `GET /companies/:id/stream` handles three cases: replay full analysis immediately if already complete, send error event if status is error, or subscribe live to Redis channel and forward all agent events. `useAnalysisStream` hook on the frontend opens an EventSource, parses all event types into state (`sections`, `thinking`, `isStreaming`, `isComplete`, `error`), and cleans up on unmount. Both services deployed and healthy.

#### Files Created
```
packages/api/src/lib/redis-pubsub.ts
apps/web/lib/useAnalysisStream.ts
```

#### Files Modified
```
packages/api/src/routes/companies.ts   (added GET /companies/:id/stream)
packages/api/src/jobs/analysis.job.ts  (emit uses publishAgentEvent)
packages/api/package.json              (build script copies ANALYSIS_CONTEXT.md to dist/)
```

#### Issues Encountered
Phase 4 oversight: `tsc` does not copy `.md` files to `dist/`, so `ANALYSIS_CONTEXT.md` would not be present at `dist/lib/agent/ANALYSIS_CONTEXT.md` in production. Fixed by updating the build script: `"build": "tsc && mkdir -p dist/lib/agent && cp src/lib/agent/ANALYSIS_CONTEXT.md dist/lib/agent/"`. TypeScript error on `subscribeToAnalysis` return type: initially typed as `Promise<() => void>` but the inner unsubscribe function is async (`Promise<void>`), so `unsub().catch()` failed. Fixed by typing as `Promise<() => Promise<void>>`.

#### Notes for Claude Code
- The `useAnalysisStream` hook requires both `companyId` and `token`. Phase 6 task 6.5 (company analysis page) must read the raw token server-side via `getRawToken()` and pass it as a prop to the `AnalysisView` client component, which passes it to the hook.
- `ANALYSIS_CONTEXT.md` is now correctly copied to `dist/` on every Railway build — this is idempotent and safe.
- The SSE route is at `GET /companies/:id/stream` and is listed in the API routes table.
- The `section_complete` event payload is `{ section: string, data: unknown }` where `data` is the section's partial `CompanyAnalysis` object (e.g. `{ tagline: "..." }` for the tagline section). The hook spreads this into `sections` state.
- Railway does not time out long-lived SSE connections as long as data is flowing; the `X-Accel-Buffering: no` header prevents nginx from buffering.

---

### Phase 6 — Analysis UI
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 6.1 — AgentThinking component
- [x] 6.2 — SectionCard component
- [x] 6.3 — SectionCardSkeleton component
- [x] 6.4 — AnalysisView component
- [x] 6.5 — Company analysis page

#### Deviations
- 6.1: Uses lucide-react icons (Brain, Search, CheckCircle, Layers, AlertCircle) instead of emoji — cleaner visual. Collapsed by default; shows pulse dot when streaming and not expanded.
- 6.2: Uses `data-section={sectionKey}` on the card div to consume the prop without lint error (`sectionKey` not needed inside the component body — filtering is done in AnalysisView). Build failed first deploy due to ESLint `no-unused-vars` on the `sectionKey` prop — fixed by adding `data-section` attribute.
- 6.3: none
- 6.4: When `initialAnalysis` is provided, passes `null` as companyId and `null` as token to `useAnalysisStream` — hook returns empty state and no EventSource is opened. The `data` const uses `initialAnalysis ?? sections` to pick the right source.
- 6.5: Pre-fix: exported `getRawToken` from `api.ts` so the server component can read the session token and pass it to AnalysisView. Error state rendered inline in the server component. Token passed as prop to AnalysisView; never sent to the client as a visible string.

#### Phase Summary
The full analysis UI is live. `AgentThinking` renders a collapsible panel with per-event icons showing the agent's tool calls, thinking blocks, and section completions in real time. `SectionCard` renders all 5 data shapes — string, string[], `{name,notes}[]`, `{category,examples}[]`, and the `{pros,cons,source_url}` feedback shape. `SectionCardSkeleton` shows animate-pulse placeholders for sections not yet received. `AnalysisView` (client component) calls `useAnalysisStream`, assembles all 13 sections in a two-column grid (bull/bear/feedback full-width), and gracefully shows skeletons while streaming. The company page is now a proper server component that reads the raw session token and passes it alongside initial analysis data.

#### Files Created
```
apps/web/components/analysis/AgentThinking.tsx
apps/web/components/analysis/SectionCard.tsx
apps/web/components/analysis/SectionCardSkeleton.tsx
apps/web/components/analysis/AnalysisView.tsx
```

#### Files Modified
```
apps/web/app/(app)/company/[id]/page.tsx   (replaced Phase 3 stub with real page)
apps/web/lib/api.ts                         (exported getRawToken)
```

#### Issues Encountered
`next build` failed on first deploy: ESLint `no-unused-vars` on the `sectionKey` prop in `SectionCard` — the prop was declared but never used in the component body (filtering happens in AnalysisView). Fixed by using `sectionKey` as the value of a `data-section` HTML attribute. Also: `jose` Edge Runtime warnings about `CompressionStream` are emitted during build — these are pre-existing warnings from NextAuth v5's use of jose and do not cause build failures.

#### Notes for Claude Code
- Phase 7 chat will need `@tanstack/react-query` — not yet installed in `apps/web`.
- `SectionCard` receives `expansionCards: ExpansionCard[]` — Phase 6 renders them inline as simple blue cards. Phase 7 replaces the inline render with the proper `<ExpansionCard>` component.
- `AnalysisView` accepts `expansionCards?: ExpansionCard[]` (default empty array). Phase 7.5 updates the company page to pass expansion cards fetched from `GET /companies/:id` (once that route includes them).
- The `getRawToken()` export in `api.ts` is used by server components to get the raw JWE token for SSE auth. It is NOT safe to pass to client components as a visible prop if the token is sensitive — but since it's an httpOnly cookie value read server-side and passed via a React prop (not exposed in HTML), it is acceptable for the SSE use case.

---

### Phase 7 — Chatbot + Expansion Cards
**Completed:** 2026-04-15
**Status:** `[x] Complete`

#### Task Checkboxes
- [x] 7.1 — Conversations repository
- [x] 7.2 — Chat route
- [x] 7.3 — ExpansionCard component
- [x] 7.4 — ChatPanel component
- [x] 7.5 — Wire expansion cards into SectionCard

#### Deviations
- 7.1: Added `expansionCards.ts` repository alongside `conversations.ts` (not in spec as separate task — required for `findByCompanyId` in the companies route and chat route).
- 7.2: Anthropic call is inline in the route handler per Context.md spec (deviation from CursorRules "no blocking Anthropic calls in route handlers"). Context.md explicitly specifies this pattern for the chat route — single Q&A turn, not a long-running agent.
- 7.3: none
- 7.4: ChatPanel uses a server action (`lib/actions.ts`) instead of calling Fastify directly — client components cannot read httpOnly cookies, so the token must be forwarded server-side via a server action. `useMutation` wraps the server action call.
- 7.5: `GET /companies/:id` now returns `{ company, expansionCards }`. `getCompany()` in `api.ts` returns both. AnalysisView manages expansion cards in local state (initialized from server props, grown by ChatPanel callbacks). ChatPanel is only rendered when `initialAnalysis` is provided (analysis complete).

#### Phase Summary
Chat is live. `POST /companies/:id/chat` takes a question, calls Anthropic with the full analysis JSON as context, extracts `{ section_key, answer }` from the response, inserts an expansion card row, upserts the conversation messages, and returns `{ expansionCard, message }`. Expansion cards are fetched server-side with the company and passed as `initialExpansionCards` to AnalysisView. New cards from chat appear immediately via state update. ChatPanel shows a spinner while pending and disables input. Server action in `lib/actions.ts` proxies the request to Fastify (reads httpOnly cookie server-side).

#### Files Created
```
packages/api/src/db/repository/conversations.ts
packages/api/src/db/repository/expansionCards.ts
packages/api/src/routes/chat.ts
apps/web/components/chat/ExpansionCard.tsx
apps/web/components/chat/ChatPanel.tsx
apps/web/components/providers/QueryProvider.tsx
apps/web/lib/actions.ts
```

#### Files Modified
```
packages/api/src/routes/companies.ts   (GET /companies/:id includes expansionCards)
packages/api/src/server.ts             (registered chatRoutes)
apps/web/components/analysis/AnalysisView.tsx  (expansion card state + ChatPanel)
apps/web/app/(app)/company/[id]/page.tsx       (destructures { company, expansionCards })
apps/web/app/layout.tsx                        (QueryProvider wrapper)
apps/web/lib/api.ts                            (getCompany returns { company, expansionCards })
```

#### Issues Encountered
TypeScript error on `ExpansionCard` type in `api.ts` — not imported before use in the `getCompany` return type. Fixed by adding `ExpansionCard` to the type re-export line. Both services deployed SUCCESS on first attempt.

#### Notes for Claude Code
- CursorRules says "no blocking Anthropic calls in route handlers" but Context.md explicitly specifies the chat route calls Anthropic directly. This is intentional — the chat is a single fast Q&A turn, not a long agent run.
- `lib/actions.ts` uses file-level `"use server"` — valid because it exports only server actions, no React components.
- Phase 8 needs: re-analyze endpoint, delete/re-analyze buttons, empty states, sidebar search.

---

### Phase 8 — Dashboard Polish
**Completed:** [DATE]
**Status:** `[ ] Not started`

#### Task Checkboxes
- [ ] 8.1 — Re-analyze endpoint
- [ ] 8.2 — Delete + re-analyze buttons in UI
- [ ] 8.3 — Empty states
- [ ] 8.4 — Sidebar company search

#### Deviations
[CURSOR WRITES THIS]

#### Phase Summary
[CURSOR WRITES THIS]

#### Notes for Claude Code
[CURSOR WRITES THIS]

---

### Phase 9 — Hardening
**Completed:** [DATE]
**Status:** `[ ] Not started`

#### Task Checkboxes
- [ ] 9.1 — Rate limiting
- [ ] 9.2 — Error boundaries
- [ ] 9.3 — Secret audit
- [ ] 9.4 — Railway production env audit
- [ ] 9.5 — Sentry integration
- [ ] 9.6 — Full user flow walkthrough

#### Deviations
[CURSOR WRITES THIS]

#### Phase Summary
[CURSOR WRITES THIS]

#### Notes for Claude Code
[CURSOR WRITES THIS]

---

## Codebase State Graph

<!--
  Cursor updates this after EVERY phase.
  Claude Code reads this before generating next phase batch.
  This is the ground truth — not Context.md, not comments, not memory.
-->

```
MODULES:
├── api/ (packages/api/src/)
│   ├── server.ts             [Phase 3] + companiesRoutes + startAnalysisWorker
│   ├── routes/
│   │   ├── auth.ts           [Phase 2] GET /auth/me (protected)
│   │   ├── companies.ts      [Phase 3] GET/POST /companies, GET/DELETE /companies/:id
│   │   └── chat.ts           [not started]
│   ├── plugins/
│   │   ├── auth.ts           [Phase 2] verifyNextAuthToken (jose JWE), req.user
│   │   ├── cors.ts           [not started]  (inline in server.ts)
│   │   └── redis.ts          [not started]
│   ├── db/
│   │   ├── schema.ts         [Phase 1] Drizzle tables per Context
│   │   ├── client.ts         [Phase 1] pg pool + drizzle (requires DATABASE_URL)
│   │   ├── migrations/       [Phase 1] applied via preDeployCommand on api service
│   │   ├── fixtures/seed.ts  [Phase 1] demo user + Vercel company + upsert
│   │   └── repository/
│   │       ├── users.ts      [Phase 2] findById, findByGoogleId, findOrCreate
│   │       ├── companies.ts  [Phase 3] create, findById, findAllByUser, updateStatus, updateError, deleteById
│   │       └── conversations.ts [not started]
│   ├── jobs/
│   │   └── analysis.job.ts   [Phase 4] real agent: running→runAnalysisAgent→complete|error
│   └── lib/
│       ├── agent/
│       │   ├── index.ts      [Phase 4] orchestrator: 13-section loop, tool-use loop, emit
│       │   ├── tools.ts      [Phase 4] web_search, scrape_url, search_news tool defs
│       │   └── ANALYSIS_CONTEXT.md [Phase 4] agent system prompt (loaded at startup)
│       ├── tavily.ts         [Phase 4] tavilySearch, tavilyNewsSearch, tavilyExtract
│       ├── scraper.ts        [Phase 4] cheerio scraper + Tavily fallback
│       ├── redis-pubsub.ts   [Phase 5] publishAgentEvent, subscribeToAnalysis
│       ├── queue.ts          [Phase 3] BullMQ Queue 'analysis'
│       ├── redis.ts          [Phase 3] ioredis client
│       └── types.ts          [Phase 1] CompanyAnalysis type only
├── web/ (apps/web/)
│   ├── app/
│   │   ├── page.tsx          [Phase 3] landing + server action → POST /companies
│   │   ├── layout.tsx        [Phase 1] root + Inter + metadata
│   │   ├── api/auth/[...nextauth]/route.ts [Phase 2] NextAuth handler
│   │   ├── api/healthz/route.ts [Phase 3] Railway healthcheck endpoint
│   │   ├── (auth)/sign-in/page.tsx [Phase 2] Google sign-in page
│   │   ├── (app)/layout.tsx  [Phase 3] sidebar shell + server-side company fetch
│   │   ├── (app)/dashboard/page.tsx [Phase 3] company grid
│   │   └── (app)/company/[id]/page.tsx [Phase 6] server component: reads token, renders AnalysisView
│   ├── components/
│   │   ├── ui/button.tsx     [Phase 1] shadcn default
│   │   ├── sidebar/Sidebar.tsx [Phase 3] sidebar shell
│   │   ├── sidebar/CompanyListItem.tsx [Phase 3] name + status badge + active state
│   │   ├── analysis/
│   │   │   ├── AgentThinking.tsx     [Phase 6] collapsible trace panel, lucide icons, pulse
│   │   │   ├── SectionCard.tsx       [Phase 6] 5 shape renderers + inline expansion cards
│   │   │   ├── SectionCardSkeleton.tsx [Phase 6] animate-pulse placeholder
│   │   │   └── AnalysisView.tsx      [Phase 6] client component, 13-section grid
│   │   └── chat/             [not started]
│   ├── lib/
│   │   ├── utils.ts          [Phase 1] cn() helper (shadcn)
│   │   ├── auth.ts           [Phase 3] + trustHost: true
│   │   ├── api.ts            [Phase 3] + getCompanies, getCompany, createCompany, deleteCompany
│   │   ├── types.ts          [Phase 3] CompanyListItem, Company, CompanyAnalysis, ExpansionCard
│   │   └── useAnalysisStream.ts [Phase 5] EventSource hook — thinking/tool/section/replay events
│   └── middleware.ts         [Phase 2] auth gate → /sign-in

API ROUTES (packages/api):
  GET  /health                 → { status: "ok", timestamp: string }
  GET  /auth/me (protected)    → { id, email, displayName, avatarUrl }
  GET  /companies (protected)  → { companies: CompanyListItem[] } (no analysis field)
  POST /companies (protected)  → 201 { company } (queues BullMQ job)
  GET  /companies/:id (protected) → { company } (full, with analysis)
  DELETE /companies/:id (protected) → 204
  GET  /companies/:id/stream (protected, ?token= auth) → SSE text/event-stream

KNOWN ISSUES:
  apps/web/app/globals.css: removed outline-ring/50 universal @apply (Tailwind v3 incompatible).
  Postgres PGDATA is at /var/lib/postgresql/data/pgdata (subdirectory to avoid lost+found conflict).
  Railway env vars TAVILY_API_KEY may still be empty — set before testing live analysis.

OPEN DECISIONS:
  None
```

---

## Pattern Log

| Pattern | Description | File | Phase |
|---|---|---|---|
| Monorepo dev | `pnpm dev` runs parallel `dev` in all workspace packages | package.json (root) | 1 |
| API health | JSON `{ status, timestamp }` for load balancers | packages/api/src/server.ts | 1 |
